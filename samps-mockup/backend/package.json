{
  "name": "samps-backend-mock",
  "version": "1.0.0",
  "description": "Backend simulado para SAMPS usando json-server",
  "main": "server.js",
  "scripts": {
    "start": "node -r dotenv/config node_modules/.bin/json-server --watch db.json --port 3001 --middlewares middleware.js",
    "dev": "npm start"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "json-server": "^0.17.4"
  }
}
